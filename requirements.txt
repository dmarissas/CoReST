# Python 3.11
#
# Install PyTorch FIRST, matched to your CUDA version (RTX 3060, driver supports CUDA 12.x):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# (CPU only:)
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# Then: pip install -r requirements.txt

torch
torchvision

# Core scientific stack
numpy
pandas
scipy
scikit-learn
matplotlib

# Single-cell / spatial
scanpy
anndata
scikit-misc        # required by scanpy's highly_variable_genes(flavor="seurat_v3")

# Image feature extraction (UNI via timm + Macenko stain normalization)
timm
torchstain
huggingface_hub
Pillow

# Utilities
joblib
tqdm

# NOTE: torch-geometric / torch-scatter / torch-sparse were removed.
# They are NOT imported anywhere in this codebase (graph_func.py uses scipy/sklearn),
# and they require version-matched prebuilt wheels that break a plain `pip install`.
# If you later add the upstream SEDR_module.py and it needs PyG, install it separately:
#   https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html
